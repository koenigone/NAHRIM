export interface WindyData {
  Win_Date: string;
  Win_Min: number;
  Win_Max: number;
  Win_Current: number;
  name?: string; // Optional property for the chart
  temperature?: number; // Optional property for the chart
}